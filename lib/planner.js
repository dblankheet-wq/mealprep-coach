import { recipeByName } from '../data/recipes';
import { packageLine } from '../data/packaging';
export const days = ['Maandag','Dinsdag','Woensdag','Donderdag','Vrijdag','Zaterdag','Zondag'];
export const defaultPlan = { Maandag: ['Kip rijst bowl','Italiaanse kip'], Dinsdag: ['Tonijn pasta','Kip curry'], Woensdag: ['Kip rijst bowl','Taco bowl'], Donderdag: ['Skyr bowl','Zalm uit de oven'], Vrijdag: ['Mediterrane couscous','Uit eten / overslaan'], Zaterdag: ['Uit eten / overslaan','Uit eten / overslaan'], Zondag: ['Skyr bowl','Kip teriyaki'] };
export const defaultState = { profile: { name:'Dominique', age:34, sex:'vrouw', height:172, startWeight:75, weight:75, goal:65, kcal:1600, protein:130, maintenance:2200, water:2.5 }, plan: defaultPlan, weights: [{ date: new Date().toISOString().slice(0,10), weight: 75 }], checkedItems: {}, pantry: {} };
export function calculateShopping(plan, pantry = {}) { const totals = {}; const cats = {}; let kcal = 0; let protein = 0; const dayTotals = {}; days.forEach(day => { let dayKcal = 0; let dayProtein = 0; plan[day].forEach(meal => { const recipe = recipeByName[meal]; kcal += recipe.kcal; protein += recipe.protein; dayKcal += recipe.kcal; dayProtein += recipe.protein; recipe.ingredients.forEach(([name, amount, unit, cat]) => { const key = `${name}|${unit}`; totals[key] = (totals[key] || 0) + amount; cats[name] = cat; }); }); dayTotals[day] = { kcal: dayKcal, protein: dayProtein }; }); const grouped = {}; const adjustedTotals = {}; Object.entries(totals).forEach(([key, amount]) => { const [name, unit] = key.split('|'); const inHouse = Number(pantry[key] || 0); const needed = Math.max(0, amount - inHouse); adjustedTotals[key] = needed; const cat = cats[name] || 'Overig'; if (!grouped[cat]) grouped[cat] = []; grouped[cat].push({ key, name, amount, inHouse, needed, unit, line: needed === 0 ? `${name}: genoeg in huis (${amount} ${unit} nodig)` : packageLine(name, needed, unit) }); }); const picnicText = Object.entries(adjustedTotals).filter(([, amount]) => amount > 0).sort(([a],[b]) => a.localeCompare(b)).map(([key, amount]) => { const [name, unit] = key.split('|'); return `${name} ${amount} ${unit}`; }).join('\n'); return { totals, adjustedTotals, grouped, picnicText, kcal, protein, dayTotals }; }
export function trend(weights) { if (!weights || weights.length < 2) return null; const sorted = [...weights].reverse(); const first = sorted[0]; const last = sorted[sorted.length - 1]; const daysBetween = Math.max(1, (new Date(last.date) - new Date(first.date)) / (1000 * 60 * 60 * 24)); const weekly = ((last.weight - first.weight) / daysBetween) * 7; return { weekly }; }
export function targetDate(weeks) { if (!weeks) return '—'; const date = new Date(); date.setDate(date.getDate() + weeks * 7); return date.toLocaleDateString('nl-NL', { day:'numeric', month:'long' }); }
